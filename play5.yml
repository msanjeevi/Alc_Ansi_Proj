---
 - name: Play to copy repos file 
   hosts: [ sun, earth, galaxy ]
   become: true
   become_user: root
   tasks:
   - name: Install vim software if it doesnt installed
     yum:
      name: vim-common
      state: absent

...
      
